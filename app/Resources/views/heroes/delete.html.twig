{% extends 'base.html.twig' %}

{% block main %}
    <div class="container body-content span=8 offset=2">
        <div class="well">
            <form class="form-horizontal" action="{{ path('hero_delete', {id: hero.id}) }}" method="POST">
                <fieldset>
                    <legend>Delete Hero</legend>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="hero_image">Image URL</label>
                        <div class="col-sm-4 ">
{#                            <input type="text" class="form-control" id="hero_img" placeholder="image URL"#}
{#                                   name="hero[imageURL]" value="{{ hero.typeId.img }}" disabled>#}
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-4 control-label" for="hero_name">Name</label>
                        <div class="col-sm-4 ">
                            <input type="text" class="form-control" id="hero_name" placeholder="Name"
                                   name="hero[name]" value="{{ hero.name }}" disabled>
                        </div>
                    </div>

{#                    <div class="form-group">#}
{#                        <label class="col-sm-4 control-label" for="hero_content">Content</label>#}
{#                        <div class="col-sm-6">#}
{#                        <textarea class="form-control" rows="6" id="hero_content"#}
{#                                  name="hero[content]" disabled>{{ hero.content }}</textarea>#}
{#                        </div>#}
{#                    </div>#}


                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-4">
                            <a class="btn btn-default" href="{{ path('hero_view', {id: hero.id}) }}">Cancel</a>
                            <input type="submit" class="btn btn-danger" name="delete" value="DELETE">
                        </div>
                    </div>
                </fieldset>
            </form>
        </div>
    </div>

{% endblock %}

